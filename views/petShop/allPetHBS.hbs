
<h1 id="riley">Riley's Animal World!</h1>
<div class="containerBg"> 
    <div class="petListCont">
        <a href="/addPet">Click here to Add New Pet!</a>
        <div> Pets</div>
        <ol>
            {{#each petInDbObj}}
            <li>
                <a href ="/pet/{{_id}}">Name: {{name}}</a>
                <p>Type: {{type}}</p>
                <p>Age: {{age}} Years</p>
                <p>Gender: {{gender}}</p>
                <p>Status: {{status}}</p>
            </li>
            {{/each}}
        </ol>
    </div>

    <div class="shopListCont">
        <a href="/addStore">Click here to Add New Stores!</a>
        <div> Shops</div>
        <ol>
            {{#each locationsInDbObj}}
            <li>
                <a href ="/shop/{{_id}}">Name: {{name}}</a>
                <a href ="/shop/{{_id}}">State: {{state}}</a>
                <p>City: {{city}}</p>
                <p>Hour Start: {{hourStart}}</p>
                <p>Hour End: {{hourEnd}}</p>
                {{!-- <ol> --}}
                {{!-- Pets Here Today: {{#each pets}} --}}
                    {{!-- <li> test --}}
                            {{!-- {{_id}}  --}}
                    {{!-- </li> --}}
                            {{!-- {{/each}} --}}
                {{!-- </ol>  --}}
                            
            </li>
            {{/each}}
        </ol>
    </div>

    <div class="saleListCont">
        <a href="/addSale">Click here to Add New Sales</a>
        <div> Sales</div>
        <ol>
            {{#each salesInDbObj}}
            <li>
                <a href ="/{{_id}}">Month: {{month}}</a>
                <a href ="/{{_id}}">Year: {{year}}</a>
                <a href ="/{{_id}}">Sales: {{amount}}</a>
            </li>
            {{/each}}
        </ol>
    </div>
</div>